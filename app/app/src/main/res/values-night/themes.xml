<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme -->
    <style name="Theme.Signsense" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>

        <!-- Background -->
        <item name="background">@color/dark.primary.bg</item>

        <!-- Primary brand color -->
        <item name="colorPrimary">@color/dark.primary</item>
        <item name="colorPrimaryDark">@color/dark.highlight</item>
        <item name="colorPrimaryVariant">@color/dark.primary.accent</item>
        <item name="colorOnPrimary">@color/dark.primary.bg</item>

        <!-- Secondary brand color -->
        <item name="colorSecondary">@color/dark.secondary</item>
        <item name="colorSecondaryVariant">@color/dark.secondary.accent</item>
        <item name="colorOnSecondary">@color/dark.highlight</item>
        <item name="colorAccent">@color/dark.secondary.accent</item>

        <!-- Status bar color -->
        <item name="android:statusBarColor">?attr/colorOnPrimary</item>

        <!-- Text color -->
        <item name="android:textColorPrimary">@color/dark.secondary</item>
        <item name="android:textColorSecondary">@color/dark.secondary.accent</item>
        <item name="android:textColorTertiary">@color/dark.highlight</item>
    </style>
</resources>